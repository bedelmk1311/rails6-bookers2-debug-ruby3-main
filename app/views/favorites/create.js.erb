$(".favorite-btn").html("<%= escape_javascript(render partial: 'favorites/favorite-btn', locals: {book: @book}) %>");
  // インスタンス変数は、favorites_controllerのcreateアクションで定義されたものが使用できます。
  // $("~") JavaScriptのセレクタを使用して特定の要素を選択 置き換えを説明している感じ
  // .html() html側　変更する要素を指定
  // ()内　部分テンプレートに@bookを渡し、'favorites/btn'`という部分テンプレートを呼び出します。

  // $("#favorite_btn_<%= @book.id %>").html("<%= j(render 'favorites/btn', book: @book) %>");
  // 短縮系
